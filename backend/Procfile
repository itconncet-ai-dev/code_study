web: python -m uvicorn src.main:app --host 0.0.0.0 --port ${PORT:-8000}
worker: celery -A src.tasks.celery_app worker --loglevel=info --concurrency=2 -Q default,document_generation,practice_generation,cleanup
